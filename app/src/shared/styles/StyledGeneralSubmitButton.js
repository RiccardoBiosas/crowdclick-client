import styled from 'styled-components'
import StyledGlobalButton from './StyledGeneralButton'

const StyledGeneralSubmitButton = styled(StyledGlobalButton).attrs({
  type: 'submit'
})``

export default StyledGeneralSubmitButton
